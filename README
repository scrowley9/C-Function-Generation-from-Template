
How to use:
(1) Create a template file in JSON form. (type the field names exactly as you see them here)
    templates - specify the start of a template
        function_names - array of function names
        return_type - function return type
        args - array of argument tuples - name and type

